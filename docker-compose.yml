services:
  api:
    build: .
    container_name: sprint-intell-api
    ports:
      - "${PORT:-5001}:5001"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
